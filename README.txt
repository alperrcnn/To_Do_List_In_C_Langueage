To-Do List - C Console Application

This is a simple console-based To-Do List application written in C.

Features

- Add new tasks
- List all tasks
- Delete tasks
- Mark tasks as completed or pending
- Saves tasks to a file (`tasks.txt`) and loads them on startup
- Clears the screen between operations

